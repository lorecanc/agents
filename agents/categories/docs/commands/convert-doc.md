---
description: Convert a document to a different format
agent: docs
---

Convert the document: $ARGUMENTS

Use doc_convert to transform it. Ask me which format I want (PDF, DOCX, Markdown, TXT) if I haven't specified.
